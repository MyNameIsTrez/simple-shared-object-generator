{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch generate_complex_so.c",
      "type": "cppdbg",
      "request": "launch",
	  "cwd": ".",
      "program": "${workspaceRoot}/a.out",
      "preLaunchTask": "Compile generate_complex_so.c"
    }
  ]
}
